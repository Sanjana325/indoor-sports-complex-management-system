import "../../styles/PlayerTables.css";

export default function PlayerMyClasses() {
  return (
    <div className="pt-page">
      <div className="pt-header">
        <h2 className="pt-title">My Classes</h2>
      </div>

      <div className="pt-placeholder">
        This page will show your enrolled classes (UI-only now). You’ll tell me the exact table + actions next 😄
      </div>
    </div>
  );
}
